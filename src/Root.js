import React from 'react';
import Form from 'Components/Form';
import GlobalStyle from 'theme/GlobalStyles';

const Root = () => (
  <>
    <GlobalStyle />
    <Form />
  </>
);

export default Root;
